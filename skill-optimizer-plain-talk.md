# Skill Optimizer 大白话版

---

## 这东西是干嘛的

你写了一个 skill（就是告诉 AI 怎么干活的一份说明书），但你不知道写得好不好。Skill Optimizer 就是帮你**自动检查和改进这份说明书**的工具。

打个比方：你写了一本菜谱。Skill Optimizer 会：
1. 先给菜谱打分（格式对不对、步骤清不清楚）
2. 再实际做一遍菜（按菜谱操作，看能不能吃）
3. 找出哪里写得不好，帮你改
4. 改完再做一遍，看有没有变好吃
5. 变好吃了就保留，没变好吃就退回原来的版本

---

## 它怎么打分

分两部分：

### 第一部分：看格式（75分）

就像老师改作文先看格式：标题写了没、段落有没有编号、有没有错别字。

| 检查项 | 满分 | 大白话翻译 |
|--------|------|-----------|
| 自我介绍 | 8分 | 别人一看就知道这个 skill 干嘛的 |
| 步骤清楚吗 | 15分 | 能照着一步步做，不会迷路 |
| 出错了怎么办 | 10分 | 不是只写正常情况，出问题有备选方案 |
| 有没有喊停 | 7分 | 关键步骤有没有让用户确认一下 |
| 指令具体吗 | 15分 | 没有"适当处理"这种废话，全是能照着做的话 |
| 引用有效吗 | 5分 | 引用的文件路径是真的存在的 |
| 整体结构 | 15分 | 有没有废话，有没有漏掉重要的东西 |

这 75 分是我直接在读你的 skill，看格式就能打出来的分。

### 第二部分：看效果（25分）

**这是重点。** 格式再好看的 skill，用起来可能一坨屎。

就像一个菜谱写得再工整，做出来的菜难吃就是不合格。

所以我设计了一套"考题"：给 skill 几个真实的任务，看它做完之后，输出对不对。

```
考题 1: "帮我把这段 JSON 转成 CSV"
  判定标准: 转出来的 CSV 能正常打开 / 数据没丢 / 特殊符号没乱码

考题 2: "这些会议笔记太乱了，帮我整理"
  判定标准: 整理的笔记有标题有日期 / 行动项都提取出来了 / 没有瞎编内容

...以此类推，大概 8-15 道题
```

每题只看**过还是没过**，不搞"大概 7 分吧"这种模糊打分。过了就是过了，没过就是没过。

然后算：**过了几题 ÷ 总题数 × 25 分 = 效果分**。

---

## 考题怎么设计——不是拍脑袋想

普通做法：随便想几个 prompt 考一考。但这有个问题——你可能只想了最常规的情况，漏了各种"意外"。

我用一个叫 **Dimension × Tuple** 的方法：

### 先找 3 个"变化轴"

比如一个会议纪要 skill，你想想它在什么情况下最容易翻车：

```
维度 1: 用户给了多少信息？
  → 信息充分 / 只有几句话 / 信息模棱两可

维度 2: 谁在提问？
  → 新手（需要手把手教）/ 老手（要干货）/ 老板（只要摘要）

维度 3: 什么会出错？
  → 一切正常 / 缺数据 / 需求互相矛盾
```

### 再排列组合

三个维度各取一个值，拼成一个测试场景：

```
场景 A: [信息充分] + [新手] + [一切正常]    → 最简单的 happy path
场景 B: [只有几句话] + [老手] + [缺数据]     → 输入很少还要干货，考验摘取能力
场景 C: [信息模棱两可] + [老板] + [需求矛盾]  → 地狱难度
...
```

大概生成 12 个组合，找你确认一下（去掉那些现实中不可能的组合），然后每个组合写一道题。

这样保证**考题是系统覆盖的**，不是拍脑袋漏掉重要场景。

---

## 判卷老师靠谱吗——先校准

你写了考题，让 AI 当判卷老师去判。

但有个问题：**判卷老师自己的水平怎么样？会不会乱判？**

### 所以要先"校准判卷老师"

从考题里抽 20 道。你先自己判一遍——**你说了算，你是标准答案**。

然后让 AI 也判一遍。对比：

```
你判的:  过  不过  过   过   不过  过   过   不过
AI判的:  过  过    过   不过 不过  过   不过 不过
             ↑错       ↑错        ↑错    (3道判错了)
```

算两个数：

- **AI 判"过"的题里，有多少你也判"过"？**（这叫 TPR，越高说明 AI 不会漏判）
- **AI 判"不过"的题里，有多少你也判"不过"？**（这叫 TNR，越高说明 AI 不会误杀）

如果 AI 85% 以上的判断跟你一致 → 这个判卷老师靠谱，可以用。
如果不一致 → 改考题的描述，让标准更明确，再试一次。
试了 3 次还是 70-80% → 就标注"不太准"，后续看分数时心里有数。

**为什么费这么大劲？** 如果判卷老师自己乱判，后续所有的优化方向都不可信。

---

## 怎么优化

上面都是"评估"。评估完之后开始真正"优化"。

### 步骤很简单

```
1. 找出最弱的地方
   → 是格式太差？还是某道考题没过？
   
2. 只改一个问题
   → 一次就改一个地方，改多了不知道哪个起效果
   
3. 改完重新跑一遍考题
   → 注意：重新评估必须用一个新的判卷老师（不能让它"记住"上次的答案）
   
4. 对比分数
   如果分数涨了 → 保留这个改动
   如果分数没涨 → 用 git 回滚，就当没改过
   
5. 分数只升不降
   → 就像只能往前转的齿轮，永远不会退步
```

### 什么时候停

- 每个 skill 最多改 3 轮
- 如果一轮都没进步 → 这个 skill 到瓶颈了，跳到下一个
- 连续两个 skill 都卡住 → 可能是时候"推翻重写"整个 skill 了（当然要先问你能不能这么干）

---

## 修什么、先修什么

不是随便修的，有优先级：

| 优先级 | 什么情况 | 怎么修 |
|--------|---------|--------|
| 🔴 最急 | 好多考题在同一个功能上挂了 | 这个功能的说明肯定有问题，重点改它 |
| 🔴 最急 | 用了这个 skill 反而比不用还差 | skill 可能管太多了，精简 |
| 🟡 重要 | 自我介绍里没写触发词 | 补上"什么时候用这个 skill"的关键词 |
| 🟡 重要 | 步骤没有编号 | 写成第一步第二步 |
| 🟡 重要 | 关键操作没让用户确认 | 插入"确定要覆盖这个文件吗？" |
| 🟢 一般 | "适当处理"这种废话 | 改成"输出为 JSON，字段如下：" |
| 🟢 一般 | 没说出错了怎么办 | 补充"如果文件不存在，提示用户" |
| ⚪ 锦上添花 | 一段太长 | 拆开 + 用表格 |
| ⚪ 锦上添花 | 话说重了 | 合并重复内容 |

---

## 一个人还是团队

优化过程中有几个角色：

- **主 agent（我）**：看格式打分 + 动手改 skill
- **判卷老师（子 agent）**：独立的，专门判考题过没过。**绝对不能是我——自己改自己判，跟考试给自己打分有什么区别？**
- **你（用户）**：每个 skill 改完之后看一眼改动，确认 OK 再继续。防止 AI 改偏了方向。

---

## 改完长什么样——成果卡片

每改完一个 skill，生成一张漂亮的卡片，上面有：

```
┌─────────────────────────────────┐
│  📊 Skill Optimizer 成果报告     │
│                                 │
│  meeting-notes                  │
│  改前: 62.5  →  改后: 87.5      │
│  通过率: 5/8 → 7/8              │
│                                 │
│  改进:                          │
│  • 补充了边缘情况处理              │
│  • Fix: test 8 极简输入现在能处理  │
│                                 │
│  判卷老师: TPR=0.90 TNR=0.85     │
└─────────────────────────────────┘
```

自动截图存成 PNG，留作记录。

---

## 和原版 darwin-skill 有什么区别

**原版的做法：**
看完 skill，每个维度打 1-10 分。"效果怎么样？嗯... 大概 6 分吧。" 说不清楚哪里不好。

**我的做法：**
设计考题 → 实际跑 → 第 3 题挂了、第 7 题挂了。查原因："哦，碰到 null 值会直接崩溃"。

**区别就是：** 原版告诉你"这个 skill 大概 60 分"，我告诉你"修掉这 3 个 bug，通过率从 53% 提到 93%——动手吧。"

---

## 实际测过，确实有用

拿两个 skill 做对比实验，darwin-skill 和 skill-optimizer 各优化一轮：

**代码类 skill（数据转换工具）：**
- 原版能过 53% 的考题
- darwin 优化后：80%
- 我优化后：100%（4 个代码 bug 全修了，darwin 一个没修）

**模板类 skill（会议笔记）：**
- 原版能过 62% 的考题
- darwin 优化后：87%
- 我优化后：100%（补了 3 个缺失的模板）

**一句话：** 代码类 skill 用我的方法优势明显（精准修 bug），模板类 skill 两个方法都可以。

---

## 所以到底怎么用

你只需要说：
- "优化所有 skill" → 全量走一遍
- "优化 xxx 这个 skill" → 只优化一个
- "评估所有 skill" → 只打分不改

工具会自动创建 git 分支、跑评估、优化、等你确认、生成报告。你只需要在关键节点看一眼、点个头就行。

---

> **核心思想就八个字：评估 → 改进 → 实测 → 保留。**
> 
> 像训练 AI 模型一样训练你的 skill——只留进步的，退回退步的。分数只升不降。

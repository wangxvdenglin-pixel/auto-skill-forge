# Skill Optimizer 改进历程复盘

## 第零阶段：从 Darwin-Skill 到 Skill Optimizer（原始升级）

### 起点

原版 Darwin-Skill 是受 Karpathy autoresearch 启发的自主 skill 优化器——评估 → 改进 → 实测验证 → 人类确认 → 保留或回滚。但其评估体系依赖主观 1-10 打分（"效果大概 6 分吧"），无法精确定位问题，也无法复现。

### 原始升级的三个层次

**层次 1：评分体系——从"人打分"到"二进制判卷"**

Darwin 的效果评估：agent 看输出 → 感觉好不好 → 打 1-10 分。这是主观判断，无法复现。

升级后：每个测试用例预先写好 PASS 条件和 FAIL 条件（都是机械可判定的陈述句），独立 judge agent 逐条对照，输出 PASS 或 FAIL。然后 pass_rate × 25 = 效果分。

关键改进：
- **可复现：** 同一套 test-suite + criteria，任何人/任何时间跑，结果一样
- **可归因：** 分数从 62.5% 涨到 75%，你清楚地知道是第 3 号和第 4 号用例从 FAIL 变成了 PASS，而不仅仅是"+6 分"
- **可校准：** judge 准不准不是靠信任，是靠 TPR/TNR 数据

**层次 2：测试设计——从"脑暴 prompt"到"Dimension × Tuple"**

原来的 test-prompts.json 依赖操作者当时能想到什么就用什么，覆盖范围随机。

升级后借鉴 generate-synthetic-data 的方法论：定义 3 个与失败模式相关的维度 → 每个维度 2-4 个值 → 笛卡尔积随机采样 ~12 个元组 → 每个元组展开为一个可判定测试用例。用户参与的时机从"审 prompt"前移到了"审维度和元组"——更早介入，但判断负担更轻。

这解决了一个关键问题：一个 skill 宣称它能处理 X 场景，但测试集里从来没有覆盖过 X，于是永远不会发现它的 X 能力是假的。

**层次 3：文档质量——meta-skill.md 原则内化**

SKILL.md 本身的写作质量提升：
- 删除所有"为什么这样做"的解释文字，只保留"做什么"——执行 skill 的 agent 不需要被说服
- 把"警告"和"建议"转成"Anti-Patterns"（一行一个）和 exception handling 的具体 fallback 动作
- 没有引言、没有引用、没有名人名言
- 所有表格、步骤、条件判断都是 agent 可以直接机械执行的

### 第零阶段遗留的问题

升级后的 skill-optimizer（旧版）引入了二进制 judge 和 Dimension×Tuple，但 Phase 2 中**同一个 agent 既改代码又评分——"自己改自己评"**。Phase 0.6 的用户校准（15-20 道题手动标注）成本过高，实践中从未被执行。整个系统对"判卷 agent 到底靠不靠谱"没有验证机制。

---

## 第一阶段：引入对抗审查（参考 ARIS）

### 灵感来源

研究了上海交大开源的 ARIS 项目（Auto-Research-In-Sleep）。其核心设计是 2-Agent 对抗模式：
- Claude Code 作为执行者（写代码、跑实验）
- GPT-5.4 作为审稿人（结构化批评 + 打分 /10 + 盲点探测）
- 循环 4 轮或直到分数达标

**ARIS 的核心洞察：一个模型无法可靠地批判自己的产出。** 同一模型的 pattern-matching 既生成内容又评估内容，导致自我强化的盲点。用跨模型家族做审稿人，引入真正的对抗压力——审稿人主动探测执行者没预料到的弱点。

### 遇到的第一道坎：没有 GPT API

ARIS 用 GPT-5.4 做审稿人需要通过 Codex MCP。我没有安装 Codex，也没有 GPT 的 API Key。如果必须依赖外部模型，整个改进就无法落地。

**解决方案：同一模型 + 独立上下文 = 等效的对抗压力。**

关键认知：对抗的本质不是"换模型"，而是"换推理链"。同一个 Claude 模型，但在**全新、独立的子 agent 会话**中，配上完全不同的角色定位，效果类似：

| 机制 | 编辑 agent | 审稿 agent |
|------|-----------|-----------|
| 看到什么 | SKILL.md 完整内容 + 诊断推理 | **仅**原始 SKILL.md + git diff + 目标问题 |
| **看不到什么** | 审稿人的存在（不知道会被挑刺） | 编辑者的推理过程、诊断数据 |
| 角色定位 | "修好这个问题" | **"找出这个改动的毛病——默认假设它有 bug"** |
| 默认倾向 | 倾向于认为自己改对了 | 敌意的、找茬的 |

**这是整个改进最重要的认知突破：隔离上下文 + 对抗性 prompt = 用同一模型实现了跨模型的盲点探测效果。**

---

## 第二阶段：合并审稿和判卷，精简流程

### 遇到的问题

加入审稿人后，Phase 2 变成了 5 步流程，加上前后阶段总计 7 个 Phase。用户反馈"整个系统太繁琐了"。

具体问题：审稿 agent（Step 3.5）和判卷 agent（Step 4）是两个独立步骤，但职责有重叠——都在判断"这个改动有没有问题"。审稿说"方向对不对"，判卷说"效果好不好"——两次子 agent 调用可以做一次完成。

**解决方案：审稿 + 判卷 合二为一。**

一个子 agent 同时扮演两个角色：
- **批评者：** 看 diff，回答"改对了吗？引入新问题了吗？有更简单的改法吗？打几分？"
- **判卷者：** 跑全部考题，返回每题 PASS/FAIL + 新通过率

流程从 5 步精简为 4 步：诊断 → 改 → 审稿+判卷 → 决定。

**关键设计决策：** 审稿和判卷合并不是为了省子 agent 调用（那是次要收益），而是让**批评和判卷共享同一个推理上下文**——审稿人发现的代码问题可以直接影响它对考题的判卷逻辑。

---

## 第三阶段：真实子 agent 测试，发现 Phase 0.6 的问题

### 实验设计

前三次实验（json-and-csv、meeting-notes、write-judge-prompt）全部使用 dry-run——主 agent 自己模拟判卷。第四次实验（error-analysis）**第一次真实 spawn 子 agent 做判卷和审稿**。

### 遇到的问题一：考题标准模糊，TNR = 0%

Phase 0.6 要求用户标注 5 道校准题，然后子 agent 也判一遍，对比计算 TPR/TNR。第一次跑的结果：

```
子 agent 判：8/8 全 PASS
我判：case 5 和 case 8 应该 FAIL
TNR = 0%  ← 严重问题
```

**根因分析：** 不是子 agent 能力不行，是**考题的 pass/fail 标准本身写得不够机械**。原标准说"skill 的 Step 1 说了要收集完整 trace，所以自然能处理残缺数据"——这是子 agent 基于隐含逻辑的推理。但我们要的是"skill 里白纸黑字写了才算"。

**解决方案：重新定位 Phase 0.6。**

从"校准判卷老师这个人"改为**"校准考题标准的机械程度"**。低 TPR/TNR 不是 judge 不好——是因为子 agent 每次都是全新实例，你没法"叫同一个人回来"。但你可以让**考题标准足够机械**，让任何一个人判都得出一样的结论。

收紧 case 5 和 8 的标准（必须有显式文本才算 PASS，隐含逻辑不算）后，第二轮子 agent 判：TPR = 100%, TNR = 100%。

**认知升级：Phase 0.6 校准的不是人，是尺子。**

---

## 第四阶段：发现跨判卷实例方差，引入方案 B

### 遇到的问题二：不同判卷 agent 判断不一致

优化完成后，用三个不同的子 agent 分别判三个优化分支。出现了无法解释的结果：

```
Darwin 判卷老师:   基线 case 4 PASS → 优化后 case 4 FAIL
Opt-Old 判卷老师:  基线 case 2 PASS → 优化后 case 2 FAIL
Opt-New 判卷老师:  基线 case 4 PASS → 优化后 case 4 FAIL
```

Case 2 和 4 在基线时明明 PASS，优化后反而"FAIL"了。但优化只加了内容没删——skill 不可能变差。

**根因分析：** 判卷老师换了人。三个子 agent 是三个独立的 Claude 实例，各自有天然不同的判断阈值。就像三个老师批改同一篇作文，有人严有人松。**你分不清"skill 变差了"还是"换了更严的老师"。**

这是之前三次 dry-run 实验从未暴露的问题——dry-run 是同一个人（主 agent）模拟所有判卷，标准天然一致。

**解决方案——方案 B：同一杆秤量前后。**

不让 Phase 1 和 Phase 2 分别 spawn 判卷 agent。优化后重判时，spawn **一个**判卷 agent，同时给它基线版和优化版的 SKILL.md，在**同一个上下文**中判两遍：

```
判卷 agent 同时收到：
  - 基线版 SKILL.md + 全部 8 道题
  - 优化版 SKILL.md + 同样 8 道题

返回：
  基线 6/8 → 优化 8/8
  Delta: +2  ← 同一杆秤量的，差值可信
```

**核心原理：** 同一个 agent 实例在同一个推理链中，标准是一致的。如果它偏严，对基线版和优化版都偏严——Delta 抵消了这种系统性偏差。如果它偏松，同样两边都偏松——Delta 还是准确的。

### 验证结果

重跑实验：

```
              基线    优化    Delta
Darwin:        6/8  →  8/8    +2
Opt-Old:       6/8  →  7/8    +1
Opt-New:       6/8  →  8/8    +2
```

**三个关键变化：**
1. 三个判卷老师基线首次一致（全说是 6/8，case 5+8 挂）——之前分开判时基线都不一致
2. Case 2 和 case 4 的假退步完全消失——不再有"换了个老师导致的假 FAIL"
3. Delta 现在可以信任——同一个老师在同一个上下文中量的前后差值

**Opt-Old 的 +1（vs +2）还暴露了一个深层问题：** 门禁放在文档末尾（Opt-Old）确实比放在开头（Darwin、Opt-New）更差——从数据上验证了审稿 agent 的判断（"gate at end is not a gate"）。

---

## 最终架构

```
Phase 0.5: 设计考题（Dimension × Tuple）
    ↓
Phase 0.6: 校准考题标准（不是校准人，是确保尺子够机械）
    ↓
Phase 1: 摸底考试
    ↓
Phase 2: 自主优化（无人干预）
    每轮 4 步：诊断 → 改 → 审稿+判卷 → 决定
    关键：Step 3 中同一个子 agent 同时判基线版和优化版
    ↓
Phase 3: 出报告
```

### 三个 Agent 的分工

| Agent | 角色 | 关键约束 |
|-------|------|---------|
| 主 agent | 组织者+编辑者 | 能看到 SKILL.md 全文 + 诊断推理。不能给自己改的代码打分。 |
| 判卷 agent | 中立判卷（Phase 0.6+1） | 看不到用户的校准标签。考题标准机械到不同人判能一致。 |
| 审稿+判卷 agent | 敌意审稿 + 同一杆秤判卷（Phase 2） | 看到原始 SKILL.md + diff + 目标问题 + 全部考题。**看不到编辑者的推理过程。** 同时判基线版和优化版，同一上下文中产生可信的 Delta。 |

---

## 核心设计决策回顾

1. **对抗审稿不依赖跨模型。** 同一模型 + 独立上下文 + 对抗性 prompt = 等效盲点探测。这解决了"没有 GPT API"的约束，且在实践中验证有效——审稿 agent（Claude 子 agent）成功发现了编辑者（主 agent）遗漏的代码放置问题。

2. **校准的是尺子，不是人。** 子 agent 每次都是全新实例，无法"复用"。但机械化的考题标准让任何实例都得出相同结论。Phase 0.6 从"校准判卷老师"重命名为"校准考题标准"。

3. **同一杆秤量前后。** 同一个判卷 agent 在同一个上下文中同时判基线版和优化版，消除跨实例方差。Delta 才是真实的改进量——不受"换了个老师"的污染。

4. **精简但不牺牲关键步骤。** 合并审稿和判卷（但不是简单的省调用，而是让批评和评判共享推理上下文），移除 Phase 2.5 的过度触发条件，移除每个 round 后的人类确认点（只保留 Phase 0.5/0.6 的人类参与）——这些都是在保持机制完整性的前提下降低复杂度。

---

## 实验数据总览

| 实验 | 对象 | 基线 | Darwin | Opt-Old | Opt-New | 子 agent 调用 |
|------|------|------|--------|---------|---------|-------------|
| 1 | json-and-csv (代码型) | 53% | 80% | 100% | — | 0 (dry) |
| 2 | meeting-notes (模板型) | 63% | 88% | 100% | — | 0 (dry) |
| 3 | write-judge-prompt (流程型) | 88% | 100% | 100% | 100% | 0 (dry) |
| 4 | error-analysis (流程型) | 75% | 100% | 88% | 100% | **7** (真实) |

**实验 4 关键数据：**
- Phase 0.6 第一轮 TNR=0%（标准模糊）→ 第二轮 TNR=100%（标准机械化）
- 审稿 agent：v1 5/10（门禁位置错误）→ v2 8/10（位置正确）
- 方案 B：消除了 case 2 和 case 4 的假退步（之前不同判卷 agent 给出矛盾结果）
- Darwin: 28 行（3 段，有重复），Opt-Old: 6 行（1 段，位置错），Opt-New: 13 行（2 段内联，无重复）
